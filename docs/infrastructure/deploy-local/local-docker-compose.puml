@startuml local-docker-compose
!theme plain
title Local Development — Docker Compose

skinparam rectangle {
  RoundCorner 10
}

cloud "Browser" as browser

cloud "Grafana Cloud" as grafana #Orange {
  rectangle "Logs · Traces · Metrics" as telemetry
}

package "Docker Compose" {
  rectangle "**web**\nDjango + Gunicorn\nPort 8000" as web #LightBlue

  note left of web
    Dockerfile:
    Python 3.12-slim
    Gunicorn (3 workers)
    WhiteNoise (static files)
  end note

  database "**db**\nPostgreSQL 17\nPort 5432" as db #LightGreen
  
  note left of db
    Volume: pgdata
    Healthcheck: pg_isready
  end note
}

browser --> web : "HTTP :8000"

web ---> db : "TCP :5432\n(PostgreSQL)"
web ---> telemetry : "OTLP/HTTP\n(traces, metrics, logs)"

@enduml
